# PTP
Peer talks to peer, based on IPFS, CLI tool.

## Install

```sh
$ npm install ptp -g
```

## Development
```
$ git clone 
$ npm install 
$ npm run dev # this should watch the change and build + start
```